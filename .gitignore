# dependencies
node_modules/
.pnp
.pnp.js

# next.js
.next/
out/

# production
build/
dist/

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# typescript
*.tsbuildinfo
next-env.d.ts

# local sqlite database
data/local.db
data/local.db-journal
*.sqlite
*.sqlite-journal

# test outputs
coverage/
playwright-report/
test-results/
.vitest-cache/
# recipe-card phase 2 — sample exported share-card PNGs for human review
_card-review/

# stray dev-server output
.dev-server.log
dev-server.log

# ux-audit run artifacts — keep findings_*.json + report*.md as the record,
# drop the screenshots (incl. per-round subfolders), the playwright-mcp cache,
# and the throwaway driver scripts + their raw data dumps from each run
.playwright-mcp/
ux-audit/*.png
ux-audit/**/*.png
ux-audit/*.cjs
ux-audit/evidence/
# stray audit screenshots the auditor drops in the repo root — no legit
# PNGs live at the repo root (app icons are under public/ and src/app/),
# so ignore them all to stop them leaking into commits.
/*.png

# UX research: keep the SYNTHESIZED reports (our original, cited work) but
# NOT the raw cleaned third-party guides (Apple/Google/NN/g excerpts) — this
# repo is public, so don't republish copyrighted source content.
docs/research/sources/

# UX audit screenshots (local reference only)
ux-audit/shots/

# Claude Code agent worktrees (local batch checkouts — never commit)
.claude/worktrees/
