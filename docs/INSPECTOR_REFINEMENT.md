# Inspector & Mobile Refinement — Ross preview feedback (2026-06-26)

Refinement pass on the just-built keystone (branch `redesign/project-panel`, NOT deployed).
Build each item as its own commit; run `npm run typecheck` + `npm run test:unit` + `npm run test:integration` (zero type errors); commit referencing the id + push to the branch. Do NOT merge to main / deploy — gated on Ross's preview review. Do NOT start a dev server while editing.

Context: the desktop project page "looks much better than it used to" per Ross — these are refinements, plus two larger reworks (mobile, new-project flow). Reuse existing primitives; match the vintage-terminal aesthetic.

## A — Desktop project page (the inspector body + action bar)
- [x] **RF-1 — Bottom action bar = a row of buttons (replace the ⋯ overflow).** Done — InspectorActionBar is now a visible button row (Focus[purple reticle] · Delete[confirm] · Save · Archive · Duplicate), sticky at the bottom; overflow menu removed. SAVE flushes the locally-held INFO edits (notes/target/ref); "Open full page" dropped (not in the named set). Turn `InspectorActionBar`'s overflow menu into a visible row of buttons across the bottom of the panel: **FOCUS · DELETE · SAVE · Archive · Duplicate**. Delete stays confirm-guarded (existing ConfirmDialog). SAVE persists the DETAILS edits (and is the create action in RF-8's new-project mode). Keep it sticky at the bottom. Acceptance: all actions reachable as labelled buttons; no hidden overflow; Delete confirms; bar works on desktop pane + (RF-10) the mobile full-screen view.
- [x] **RF-2 — Focus affordance: purple + target icon.** Done — shared `FocusReticleIcon` kit primitive (purple crosshair+circle), wired into both the desktop table row + mobile card focus actions; action-bar usage lands with RF-1. The Focus button/icon must NOT be red — recolor to **purple** (use the existing purple token). Redesign the icon to a **target/crosshair**: a `+` (plus) with a **circle at the center** (a reticle). Apply the SAME icon everywhere Focus appears: the project-page action bar AND the projects-table row action. Acceptance: no red Focus affordance remains; reticle icon renders crisply at row size (≥24px glyph) and bar size.
- [x] **RF-3 — Delete on the projects-table row.** Done — confirm-guarded red 🗑 Delete action after Focus on both the desktop table row (≥44px via after: expander, gap-2 spacing) and the mobile card; one shared ConfirmDialog, deletes via deleteProject + refresh. Add a **Delete** row action immediately after the Focus icon in `ProjectsTable` rows (both desktop table + mobile cards). Confirm-guarded; ≥44px tap area + ≥8px spacing (keep MUX-004's standard). Acceptance: each row can delete its project with confirm.
- [x] **RF-4 — Remove the redundant in-panel project title.** Done — dropped the big green EditableTitle + "% complete" line + the old red pixel FocusTargetButton from the body. Rename relocated into DETAILS as an editable "Name" Input (commits on blur/Enter via updateProjectName). The standalone /projects/[id] page keeps its own compact breadcrumb+h1 header so the document still has a heading. Drop the large green/blue project title inside `ProjectWorkspaceBody` (the breadcrumb bar "DASHBOARD ▸ PROJECT · <name>" already names it). PRESERVE rename: move the inline-editable name into the DETAILS section as an editable field (or make the breadcrumb title editable — implementer's call, keep it discoverable). Acceptance: no duplicate title; the name is still editable + persists.
- [x] **RF-5 — Wider side panel.** Done — InspectorPane is now w-[840px] (880px at 2xl), capped at max-w-[60vw] so the left projects table keeps a usable width at ~1280px (pane ~768px) and never overflows; the table retains its own overflow-x-auto. Increase the desktop inspector width from `max-w-2xl`/672px to roughly **820–880px** (`InspectorPane` + the panel width). Keep the table usable on the left at common desktop widths; collapse to a sensible min before it crowds. Acceptance: panel visibly wider; no horizontal overflow of the left table at ~1280px.
- [x] **RF-6 — Compact PROGRESS summary strip at the TOP.** Done — a dense one-line trio above DETAILS: # total · ✓ complete (green) · 🕒 time (00:00). The detailed per-sub-project status dropdowns + done/total steppers + roll-up stay in the collapsible PROGRESS section below. Add a small horizontal summary row at the top of the body (above DETAILS): icon + number trios — **`#` total models/units · ✓ completed · 🕒 time spent (00:00)**. Small/dense (not a big section). Keep the detailed per-sub-project status dropdowns + done/total steppers in a (collapsible) PROGRESS section below. Acceptance: top strip reads at a glance in 3 icon+number stats; detailed controls still available below.

## B — New-project flow
- [x] **RF-8 — Create opens the project page directly (no mini-form).** Done — removed NewProjectPanel; "+ New Project" opens CREATE mode (CreateProjectView) in the same inspector shell (desktop pane / mobile surface) with blank Name/Type/Count. SAVE→"Create" calls createProject (name-required inline validation, nothing persisted until then); on success the new id flows back via onProjectCreated→openProjectId and the auto-open effect swaps create→edit. Tour deep-link (?tour=create) now opens create mode. e2e create helpers updated. Remove the pared-down `NewProjectPanel` name+model-count form. "+ New Project" opens the project page/panel in **create mode** with blank/default fields; the bottom-bar **SAVE** (RF-1) creates the project (calls `createProject`) and transitions the panel to normal edit mode for that new id. Until SAVE, no project row is persisted (hold form state client-side). Validation (name required) surfaces inline on SAVE. Acceptance: clicking "+ New Project" lands on the full project page; filling fields + SAVE creates it and it appears in the table; cancel/close discards with no orphan row.

## C — Calendar
- [x] **RF-9 — Tighten the calendar height.** Done — MiniCalendar interactive day cells dropped from a 44px visible min-height to a compact aspect-square (~¼ the row height), with the 44px tap target restored via an invisible after: expander (MUX-003 hit area preserved). Applies everywhere MiniCalendar renders (dashboard rail + the RF-11 mobile planner). The month grid is too tall — reduce the **vertical spacing between number rows to ~¼ of current** (row height / gap). Applies to PlannerCalendar (dashboard + the mobile planner in RF-11). Keep tap targets usable (MUX-003 set 44px; if shrinking rows drops below, keep the hit area via padding/`after:` expander rather than visible height). Acceptance: calendar is visibly shorter; dates still tappable ≥44px effective.

## D — Mobile (replaces the bottom-sheet detent behavior with a simpler model)
- [x] **RF-10 — Project tap = full-screen takeover + back button.** Done — retired the detent/drag bottom sheet; the mobile project view is now a full-screen (100dvh) takeover with a top-left back chevron (aria-label "Back") that calls onClose → ProjectPanelStack's Phase-4 history (pops one sub-project tier, then closes; OS Back identical). Focus trap + labelled control kept; RF-1 action bar sticky at the bottom of the scroll body. No detent/handle code left (file kept as ProjectBottomSheet.tsx for import stability, aliased ProjectMobileSurface). On mobile, retire the `ProjectBottomSheet` detent/drag UI. Opening a project shows a **full-screen** project view with a **back button (chevron) in the top-left corner** that closes it (wire to the existing onClose + Phase-4 history so OS Back also works, popping one sub-project tier first). The action bar (RF-1) sits at the bottom. Keep the focus trap + labelled control. Acceptance: mobile project open is full-screen, top-left back returns to the dashboard; sub-project drill + Back pops one tier then closes; no leftover drag-handle/detent UI.
- [x] **RF-11 — Pare down the mobile dashboard + a planner screen.** Done — RightRail (full-width calendar + activity) is now hidden below xl, so the mobile dashboard shows only the project cards. The bottom Upcoming-Events area becomes, on mobile, a tappable bar (shows the next event) that opens a full-screen PlannerScreen (PLANNER calendar + "+ Date" + ACTIVITY TRACKER) with a top-left back chevron. Desktop (xl+) keeps the informational ticker + inline RightRail as-is. Mobile dashboard body shows ONLY the project cards. Remove the full-width calendar + activity tracker from the mobile dashboard. Add a bottom **"Upcoming Events" bar**; tapping it opens a **full-screen planner view** (the calendar + "+ Event" + activity tracker) with a **back button top-left** to return. Desktop keeps the RightRail as-is. Acceptance: mobile dashboard = cards + Upcoming-Events bar only; the bar opens a full-screen planner with back; no full-width calendar/activity on the mobile dashboard.
- [ ] **RF-12 — Mobile cards: add priority + completion bar.** The mobile project cards are missing the **priority** indicator and the **completion progress bar** the desktop table row shows. Add both to the card (reuse the table's priority chip + completion bar). Acceptance: each mobile card shows its priority + a completion bar matching the desktop row.

### Notes / judgment calls (flagged for Ross)
- Rename relocated into DETAILS (RF-4) since the big title is removed.
- Mobile goes full-screen + back (RF-10), retiring the detent sheet built in the keystone capstone — Ross's explicit preference.
- New project = blank page + SAVE, no draft row persisted until SAVE (RF-8).
- "Whole page feels messy" is addressed structurally by RF-4 (no dup title) + RF-6 (compact progress) + RF-1 (clear button bar) + RF-5 (wider). If it still reads busy after, do a spacing/hierarchy polish pass and flag for review.
