# UI Polish — Wave Plan (milestone-builder roadmap)

Consolidated from three UX audits (2026-06-24). Source-of-truth detail + screenshots live in:
- `mobile-ux-audit/findings.json` (MUX-*, compliance) + `mobile-ux-audit/shots/`
- `mobile-ux-audit/opportunities.json` (MOP-*, strategy) + `shots-strategic/`
- `ux-audit/desktop-production-opportunities.json` (DOP-*, desktop design) + `shots-desktop-prod/`

**How to run:** milestone-builder picks the next unchecked `- [ ]` item, implements to the project's conventions, runs `npm run typecheck` + the test suite, and commits/pushes per item if green. Work waves top-down. Items tagged **[ARCH — needs Ross]** are structural/design calls: milestone-builder should halt and surface a proposal rather than auto-implement.

**Guardrails:** strict TS, zero type errors; match neighboring patterns; no new deps without need; do NOT start a dev server while editing (shared `.next` corrupts). Branch: `redesign/project-panel` — commit/push here; do NOT merge to main (Ross reviews first).

---

## Wave 0 — Verify suspected bugs (do first; cheap, decides priority)
- [x] **DOP-010** — verified — not a bug: the typed name always persists. Every entry routes to `/recipes/new`, which pre-fills the name *value* as "Untitled recipe" (page.tsx `blankRecipe`); the editor binds `recipe.name` straight into `saveRecipe({ name })` → `createRecipe`/`updateRecipe` (both `name.min(1)`), so a typed name is never dropped. "Untitled recipe" only persists if the user never edits the default — expected, not a name-drop. (Minor UX nit, out of scope: that default could be a placeholder instead of a value.)
- [x] **DOP-004** — verified — not a bug (dev-fixture artifact): summary count and projects table share one source. `DashboardClient` computes `summary = deriveDashboardSummary(projects, …)` and passes the *same* `projects` array to `ProjectsTable`; both originate from `data.projects` (`buildProjectTree(listAllProjects(userId))`). `deriveDashboardSummary` is a pure fn on its argument — it does not read mock fixtures despite its file path. Any mismatch was the signed-out preview rendering pure fixtures. No real mismatch possible.

## Wave 1 — Quick comprehension & polish wins (trivial/small, high-confidence)
- [x] **DOP-006** — Added `WelcomeCard` (src/components/dashboard/WelcomeCard.tsx), a dismissible terminal-MOTD `Panel` (label `SYS ▸ WELCOME`, green accent) at the top of the dashboard: one-line "what this is", three start-here jump-offs (Create your first project → `?tour=create`, Browse Library, Build a Recipe), and a "▸ Take the tour" button calling `useTour().start()`. Dismissal persists in localStorage (`mm.welcomeDismissed`, separate from the tour's own seen-gate). Create label intentionally distinct from the PROJECTS panel's "+ New Project" so e2e selectors stay unambiguous.
- [x] **DOP-007a** — Renamed nav label "RECIPE" → "RECIPES" (src/components/shell/nav.ts MAIN_NAV; `key` stays `recipe`).
- [x] **DOP-007b** — Added `//`-comment-style descriptors via the existing `PageHeader` tagline on Library ("every paint on the market — mark what you own/want"), Collection ("only the pots & models you own"), Focus, Tools, Recipes. Single line each; reused PageHeader so no layout regression.
- [x] **DOP-016 / MUX-013** — verified — not an app bug: code/DOM trace found NO decorative "N" glyph in the app (no CSS `content`, no badge asset in `public/`, SidebarRail is plain flexbox, ReportIssueButton has no overlapping decoration) — matching the mobile auditor's own live probe (`stickyEls` empty). The circular "N" is the **Next.js dev/preview indicator**, which does not ship to a production build, so real users never see it overlap "REPORT AN ISSUE". Still moved it to bottom-right via `devIndicators.position` in next.config.ts so it stops contaminating dev + Vercel-preview screenshots; production unaffected.
- [x] **MUX-009** — Added a contrast scrim + bold to the color-wheel hex caption (`captionScrim` in src/lib/color.ts → text-shadow halo opposite the chosen text tone) so the on-swatch hex stays AA-legible on saturated mid-tone reds where pure white/black alone dips under 4.5:1. Kept the YIQ `readableText` colour choice (its contract is unit-locked). Tests added in readableText.test.ts.
- [x] **MUX-011** — Cut /sign-in CLS at its source: the `BootSequence` type-on (AuthView) started at 0 visible lines and grew its box as lines appeared, shoving the form down (measured 0.089). Now reserves full final height up front via a visually-hidden height-only spacer (one empty line-box per line) in a 1-cell grid; animated lines render over it in the same position. Spacer carries no searchable text so a11y/text queries still match only the real copy. No visual regression (panel is fixed `max-w-sm`).
- [x] **MOP-012** — Hid the "⤢ Open full page" inspector action below `md` (`hidden md:inline-flex`) in ProjectWorkspaceBody — the SlideOutPanel is `w-full` (full-bleed) under its `max-w-2xl` cap on mobile, so the action only earns its place on desktop where the panel is a capped side column.
- [x] **MUX-012** — verified — already handled: `next.config.ts` `redirects()` 308-permanent-redirects `/projects` → `/dashboard`, so the browser URL becomes `/dashboard` (no URL/title mismatch). No `(app)/projects/page.tsx` exists to shadow it, and no in-app `<Link>`/`push("/projects")` navigates there (only `revalidatePath("/projects")` no-ops). Audit predates the redirect. Full resource-list page remains DOP-017 (Wave 4, ARCH).

## Wave 2 — Mobile compliance fixes (touch targets, semantics, reflow)
- [x] **MUX-003** — Month chevrons (PlannerCalendar nav + the date-picker popover) → `h-11 min-w-11` (44px). MiniCalendar interactive day-buttons → `min-h-11` (44px tap height) by dropping the `aspect-square` lock on the interactive variant only, so the 7-col grid keeps its `max-w-[170px]` cap horizontally while gaining thumb height; read-only dashboard cells stay a tight square.
- [x] **MUX-004** — Row-action buttons (`+` add-sub, `◎` focus) bumped `h-6 w-6`→`h-7 w-7`, gap `gap-1`→`gap-2` (8px), and each carries an invisible centered 44px `after:` tap-area expander so the touch target clears the WCAG floor without the 24px glyph bloating the dense desktop row. Role/aria-label selectors unchanged (e2e safe).
- [x] **MUX-007** — Sub-project tab cells now `min-h-11` (44px) with `gap-2` (8px) between the label tap area and the close ×, and the × is a 24px (`h-6 w-6`) centered hit area. (ProjectPanelStack.tsx)
- [x] **MUX-006** — Real WAI-ARIA tabs: `role=tablist`(+`aria-orientation`)/`role=tab`(+`aria-selected`,`aria-controls`)/`role=tabpanel`(+`aria-labelledby`), roving tabindex (only the active tab is tabbable), Arrow/Home/End keyboard nav with focus follow, and a non-colour active indicator (leading `▸` marker + `border-b-2` underline, not colour alone). Matches the existing RecipePaintPicker tab pattern, extended with the roving-tabindex/arrow-key model.
- [x] **MUX-008** — Root cause was the fixed `text-title` (~44.5px) pixel font: the LandingView hero copy ("Plan your projects." 19 chars) overran the ~272px content box at 320px. Swapped the hero + the pricing `<h1>` to `clamp(min, vw, var(--text-title))` so they scale down on narrow phones and settle at full size on wider screens; also made PublicHeader wrap (`flex-wrap` + tighter mobile gaps) so the logo+nav row can't force horizontal scroll. No `<br/>`/copy change.
- [x] **MUX-010** — SlideOutPanel (the inspector's shell) now renders a bottom-anchored secondary dismiss: the caller's footer if provided, else a default full-width `✕ Close` button in the thumb zone — filling the 3.5rem the body height already reserved for a footer (no layout shift). Bumped the top-right ✕ to a 44px (`h-11 w-11`) hit area. Esc-close already wired (onKey) + overlay-tap close confirmed; the distinct accessible name keeps the existing `Close panel` e2e selector unambiguous. (Browser/OS Back→history-pop is the separate Wave-3 MOP-004 item.)
- [x] **MUX-001** — Persistent **bottom nav** (`src/components/shell/BottomNav.tsx`) shown < 840px: 4 labelled glyph tabs (Dashboard · Library · Collection · Tools) + a **More** entry opening Focus/Recipes/Account/Tutorial in the shared SlideOutPanel. Rail escalates at ≥840px (`SidebarRail` + `MobileTopBar` retargeted `md:`→`min-[840px]:`); MobileTopBar is now a logo-only header (its hamburger menu was fully superseded). Tour anchors preserved: the 4 primaries carry their real `data-tour`; the overflow anchors (`nav-recipe`, `nav-focus`) are mirrored as zero-footprint spans pinned over the More button so those steps spotlight "More" (where the painter finds them) instead of dead-centring; `nav-tutorial` stays on the replay button inside the More sheet. Safe-area inset respected; `<main>` reserves the bar's height < 840px (no content hidden, no layout shift). Updated `qa_mobile_flows.spec.ts` M6.1 to drive the bottom-nav tabs + More sheet (old "Open menu"/"Navigation" hamburger selectors were genuinely obsolete).
- [x] **MUX-002 / MOP-003** — Cards on mobile, table on desktop (`ProjectsTable.tsx`). < 600px each project renders as a stacked card (title + TYPE chip + STATUS badge + completion % progress bar + SwatchStrip + the same `+`/`◎` row actions, sized up to h-9 in the card); ≥600px keeps the existing table byte-for-byte (wrapped `hidden min-[600px]:block`). Both views share one `renderRows`/`renderCards` pair off the same expand/add-sub state + handlers — zero duplicated logic; the tree is preserved via depth `marginLeft` + the same caret. No horizontal overflow at 375/320px (cards are full-width, titles `break-words`, indent shrinks width rather than pushing it).
- [x] **MUX-005** — Swept post-MUX-001: no primary CTA is stranded at the top with no reachable alternative. Dashboard's "+ New Project" sits below the projects table; the Recipe index duplicates its create at the empty-state action + bottom-of-table button; Library/Collection top actions are filters/toggles, not creates. Adding a sticky-bottom CTA now would collide with the new bottom nav. No change needed — satisfied by MUX-001.

## Wave 3 — The inspector keystone (DOP-001 + MOP-001/002/004/005 converge)
> DECIDED (Ross 2026-06-25): **full keystone — built incrementally**. Ship the stepping-stones below as separate committed increments FIRST, then the bottom-sheet (mobile) + master-detail (desktop) capstone. The capstone (MOP-001/DOP-001) is **blueprint-gated**: an architect drafts the implementation blueprint, Ross approves it, THEN it's built. This is the branch you're on — keep each step reviewable/revertible.
- [x] **MOP-002** — Sticky bottom action bar in the inspector; demote Archive/Duplicate/Delete to an overflow menu so the destructive verb leaves the thumb-rest zone. (Stepping stone; high value, medium effort.) — Extracted `InspectorActionBar` (`src/components/dashboard/InspectorActionBar.tsx`): Focus stays the prominent 44px primary; Open-full-page/Archive/Duplicate/Delete demoted into a `⋯` `role=menu` popover (outside-click + Esc dismiss, no focus trap). Delete still routes through the existing ConfirmDialog. Bar is `sticky bottom-0` inside the scroll body. `⤢ Open full page` stays desktop-only (MOP-012 preserved via `desktopOnly`).
- [x] **MOP-005 / M4 decision** — Replace the five always-expanded inspector sections with collapsed sections / a segmented tab rail (Sub-projects + Progress open by default). Matches the locked "mobile collapsed sections" decision. — `SectionBox` → `CollapsibleSection` (local `useState`, not native `<details>`): below `md` each section collapses behind a chevron toggle, default-open SUB-PROJECTS + PROGRESS (M4 call), DETAILS/RECIPES/INFO collapsed; on `md`+ every body is `md:block` (always expanded) with the toggle `md:hidden`. Chevron rotation rides the global `prefers-reduced-motion` reset (instant snap for reduced-motion). Added a mobile-only `<nav>` quick-jump rail (horizontally scrollable section anchors) that `scrollIntoView`s and force-opens a collapsed target before jumping.
- [x] **DOP-002** — Two-up the inspector body on desktop (use the width instead of one narrow column). — Created `useBreakpoint` (`src/hooks/useBreakpoint.ts`, `useSyncExternalStore`-backed `useMediaQuery` + `useIsDesktop`, SSR snapshot `false`) and `InspectorPane` (`src/components/dashboard/InspectorPane.tsx`, persistent in-flow right column `w-[672px] border-l`, no scrim/fixed, non-modal, only the close button is labeled — matches SlideOutPanel's visual language). `ProjectPanelStack` now picks the shell by breakpoint: `InspectorPane` on md+, `SlideOutPanel` below. `DashboardView` becomes a top-level master-detail flex row; when `inspectorOpen && isDesktop` the pane occupies the right column and `RightRail` steps aside (no dimming, table stays visible).
- [x] **MOP-004** — Sticky drill breadcrumb / back chip for sub-project tabs; ensure OS Back pops one tier, not the whole sheet. — `ProjectPanelStack` now mirrors the drill stack into browser history: `pushState({mmInspector})` on open, `pushState({mmInspector,mmSubProject})` on sub-push, a `popstate` listener that pops one tier (or closes at the root) and CRITICALLY passes through untouched when `event.state?.mmInspector` is falsy (never interferes with Next router navs). User close unwinds our pushed entries cleanly via `history.go(-depth)`, guarded by a numeric `unwindingRef` countdown so the programmatic popstates don't double-handle. Breadcrumb now shows depth (`DASHBOARD ▸ PROJECT ▸ SUB…`) per active tab tier, in both the desktop pane and mobile shell.
- [x] **MOP-001 / DOP-001** — [ARCH — needs Ross] The keystone: inspector → true bottom sheet on mobile (grab handle, swipe-down + fallback, peek, thumb-reachable close) AND desktop master-detail (stop dimming half the screen for a 672px column). Schedule deliberately; the items above fold into it. — Built per the Ross-approved `docs/INSPECTOR_KEYSTONE_BLUEPRINT.md`. Extracted `useFocusTrap` (`src/hooks/useFocusTrap.ts`, shared by SlideOutPanel + the sheet, MM-24 ref pattern preserved). Created `ProjectBottomSheet` (`src/components/dashboard/ProjectBottomSheet.tsx`): peek `50dvh` / full `92dvh` detent state machine, pointer-drag with 80px+velocity-flick snapping, iOS left-edge guard (≥40px), grab handle + visible labeled close (D.2, never gesture-only), scrim-tap step-down/dismiss, `dvh` heights + `touch-pan-y` body / `touch-none` handle (B.3/risk-5), `role=dialog aria-modal`, reduced-motion via the global reset. `ProjectPanelStack` renders the sheet below md (desktop two-pane from DOP-002 covers ≥md); SlideOutPanel stays the MobileTopBar nav surface, unchanged behaviour now via the shared trap.

## Wave 4 — Under-built pages catch up to the good ones
- [x] **DOP-009** — Recipe list now has a Table/Cards `SegmentedToggle` (Library's view-toggle pattern). Table rows gained a compact swatch-preview line under the name (reuses `Swatch`); new `RecipeCard`/`RecipeCardGrid` (src/components/recipe/RecipeCard.tsx) lead each card with a full-width ordered-scheme preview strip + a 6-swatch echo, then name, paint-count, assign Listbox + Share. Cards open the recipe to edit (per-paint quick-edit stays a table-cell affordance).
- [x] **DOP-011** — New `SchemePreview` + `EmptySchemeExample` (src/components/recipe/SchemePreview.tsx). The SLOTS panel now shows a **live ordered-scheme preview** (labelled base→highlight swatch strip + indexed legend) that updates as you build, and the empty state is a worked three-layer example (Base/Layer/Highlight, decorative — never persists) instead of one line of text. Reuses `Swatch` + `readableText`.
- [x] **DOP-012** — The two collections (PAINT/MODEL) now sit in a side-by-side `xl:grid-cols-2` grid (`items-start`, stacked below xl) instead of stacking full-width. A truly-empty collection renders a compact `EmptyState` block (no min-w-760 header-only table → no wide scrollbar/tall void); the wide scrolling table + footer "+ Add" only appear once there are rows. In-table empty branch reduced to the filter-only case. Stale comment in qa_collections.spec.ts refreshed (`.first()` selector still valid).
- [x] **DOP-008** — ColorMapRail (the clickable hue rail) now gives live hover feedback — a tracking guide line + a readout of the hue under the cursor (degrees + band name via `hueName`) so the click target is legible before committing — and each coverage dot carries a `title` naming its paint (name/brand/own-wish). PaintListTable swatch tooltip upgraded to name the paint (parity with the grid SwatchWall, which already had hover tooltips + hover-scale). Rail click-to-jump behaviour unchanged.
- [ ] **DOP-003** — Fill the dashboard's empty lower half with purposeful content (e.g. a "continue painting" tier) — page ends at ~55% viewport.
- [ ] **DOP-014** — [ARCH — needs Ross] Kill the app-wide ~270px right-gutter; pick one consistent grid intent across pages.
- [ ] **DOP-017** — [ARCH — needs Ross] Give projects a real resource-list page (sort/filter/bulk) for the power-user audience; `/projects` currently === dashboard.
- [ ] **DOP-005** — [ARCH — needs Ross] Untangle FOCUS vs PLANNER vs `/planner` naming + give the calendar a clear home. Naming/IA decision.
- [ ] **DOP-015** — Surface existing cross-feature bridges (Wheel→Recipe, Library→paint-pick) at the moment of need.
- [ ] **DOP-013** — Fill the Color-Wheel's lower-right void with more matches (hold the rest of the layout — it's already good).
- [ ] **MOP-011** — Stat strip: reflow the 4-up to 2×2 on mobile (overlap at 375px); keep the "N" mark off the action row.

## Wave 5 — Mobile delight & PWA (beyond-minimum)
- [ ] **MOP-007** — Optimistic swipe-to-progress / tally increment on PROGRESS rows with haptics (the most-repeated painter action) + visible button fallback (WCAG 2.5.7).
- [ ] **MOP-006** — Finish the PWA: apple meta tags, install nudge, offline paint-library caching for table-side use.
- [ ] **MOP-013** — Section-shaped skeletons + optimistic edits instead of "▸ Loading…" text.
- [ ] **MOP-008** — [ARCH — needs Ross] Context-aware bottom-right quick-add FAB (project/sub-project/recipe). FAB is a deliberate app-wide choice — confirm.
- [ ] **MOP-009** — Extend the Color-Wheel's direct-manipulation language to the other tools + recipe paint-picking.
- [ ] **MOP-010** — Register as a Web Share Target + clipboard-detect to extend the paste-first collection capture.

---

### Notes for the runner
- Waves 0–2 are largely autopilot (small, testable). Wave 3's keystone and every **[ARCH — needs Ross]** item should HALT with a short proposal — they're design decisions, not mechanical edits.
- After each item: `npm run typecheck` + `npm run test:unit` (+ integration where touched). Commit per item with a `fix(ux):`/`feat(ux):` message referencing the finding id (e.g. `fix(ux): raise color-wheel hex contrast to AA (MUX-009)`).
- Re-verify any positional/overlap claim with a live DOM probe before "fixing" it (the audits ran as webdrivers; a couple of findings are low-confidence).
