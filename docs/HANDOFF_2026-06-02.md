# Mini Manager — Handoff Prompt (2026-06-02)

**Paste this into a fresh Claude Code chat to continue.** This is a complete state snapshot — the new session will have full context without trawling history.

---

## You're picking up Mini Manager

A paint-planning + wargaming companion web app for miniature painters. Cross-brand paint library (7,144 paints / 40+ brands), recipes, projects, wishlist, tools (Match / Gradient / Wheel / Eyedropper). Live at https://miniaturemanager.vercel.app. Repo: github.com/ProbablyMaybeNo/mini-manager.

**Working dir:** `D:\AI-Workstation\mini-manager` (standalone repo, **NOT** inside Antigravity). When dispatching agents, explicitly point them here — they've otherwise defaulted to the parent path and lost their way.

**Stack:** Next.js 16 App Router · React 19 · TypeScript strict · Tailwind v4 (`@theme` tokens, no raw hex in components) · Drizzle ORM · libsql (Turso prod, SQLite local) · NextAuth v5 credentials (username + password, no email required at sign-up). Tests: `npm test` (vitest unit + integration) + Playwright E2E.

**Production state (last push 8f7841d, 2026-06-02):**
- Tests: 1587 passing / 1 skipped
- Typecheck: clean
- Phases shipped: 1-14 plus Phase 10 milestones P10.1-P10.3 (Stripe SDK + schema + plan constants + /pricing page)
- 11 audit rounds completed; last verdict (Round 11) was 🟢

## What's live (don't re-build any of this)

- Auth (sign-up / sign-in / forgot-password) with autocomplete locked
- Library (7,144 paints across 40+ brands), ΔE/MATCH tool, harmony picker, gradient ramp, eyedropper with photo upload (extracts colors from mini photos — Ross briefly forgot this exists)
- Recipes with zones + step grid + per-step painting notes + step-level paint cards
- Projects with Army > Unit hierarchy (Single Model + Model + named-models all folded into Unit in P13.4); delete-project with cascade
- Wishlist (kit/box adds + manual entry)
- **FOCUS section on /projects** — pinned recipe + per-step notes + stopwatch + paint_sessions table + multi-recipe tabs
- **PLANNER section on /projects** — calendar (events/tournaments) + activity stream + streak counter + activity heatmap (90-day grid) + inspo gallery (external URL pastes only)
- /pricing page (4 tier cards: Free / Pro Monthly $4 / Pro Lifetime $36 / Founder $26)
- Free-tier hard caps wired (1 project / 1 recipe / 3 wishlist) with "Upgrade →" link inline on error
- Solid-fill Button discipline (P13.1) — no `[ ]` brackets anywhere on action buttons; cyan banned from buttons

## Locked decisions (don't relitigate)

- **PLANNER** is the name of the second dashboard section (not Campaign, not Studio).
- **Inspo images = external URL pastes** (Pinterest / IG / ArtStation). No Vercel Blob, no S3, no upload pipeline.
- **Named models dropped entirely.** No `named_models` table; individuals are 1-model Unit sub-projects if needed.
- **Pricing: $4/mo · $36 lifetime · $26 founder.** Ross may still revisit but it's the current commit. Pricing decisions wait for real user signal — don't push for changes pre-launch.
- **No "Coming Soon" feature teases.** Ship features as features. Free/paid gating UI lands in Phase 15+ alongside Stripe.
- **Cross-device is the BrushForge wedge.** BrushForge is mobile-only at $15 lifetime. Mini Manager is web (phone + tablet + desktop) with a broader feature scope (army hierarchy, FOCUS, PLANNER, stopwatch, BattleScribe import). Don't compromise desktop chasing mobile.
- **Photo-to-palette already exists** (`/tools/eyedropper` with DropZone). Do NOT propose to build this.

## Open work

### Blocked (waiting on Ross)
- **P10.4-P10.8 Stripe wire-up** — Ross created 3 Stripe products (Pro Monthly $4 recurring, Pro Lifetime $36 one-time, Founder Edition $26 one-time) in test mode. Needs 4 env vars in Vercel before dispatch: `STRIPE_PRICE_PRO_MONTHLY`, `STRIPE_PRICE_PRO_LIFETIME`, `STRIPE_PRICE_FOUNDER`, `STRIPE_SECRET_KEY`. (`STRIPE_WEBHOOK_SECRET` comes after P10.5 deploys + webhook is registered.) See `docs/PHASE10_PLAN.md`.

### Next-up phases (in priority order — already planned in docs/)

**Phase 15 — Mobile Excellence + FOCUS Completion** (planned, see `docs/PHASE15_PLAN.md`):
- **P15.0 FOCUS completion (FIRST — Ross flagged FOCUS shipped as display, not companion).** Add: current-slot active indicator, step-level done-checkboxes (new `recipe_step_completion` table), inline quick-action buttons (Bump Paint / Bump Prime / Advance Slot), project-state pill in FOCUS header, recipe completion %.
- P15.1 PWA install (manifest + service worker + icons)
- P15.2 Touch-target sweep
- P15.3 Mobile audit + page-by-page polish
- P15.4 Offline reads via service worker
- P15.5 Round 12 mobile audit

**Phase 16 — Tier 1 Feature Gaps (NEW DIRECTION as of 2026-06-02 — needs plan written):**
- **F.1 Heat-sink color grid** — replace the existing activity heatmap on /projects PLANNER with a paint-collection coverage grid. Squares for every paint in the library, sorted by hue. **Green border = owned, yellow border = wishlisted, no border = not owned.** Tap an empty cell → "candidate paints to fill this gap" → wishlist add. Visualization Ross designed himself. Replaces, doesn't supplement, the activity-cadence heatmap. The streak counter + activity stream stay.

**Phase 17 — Tier 2 Dashboard Polish** (sketched only, not planned):
- D.1 Pinned recipes (3-5 army-wide reusables on dashboard)
- D.2 Next-action card ("3 units at BUILT, ready to prime — pick one")
- D.3 Tournament countdown (events.event_date → dashboard nudge when within N days)

**Phase 18 — Tier 3 (queued, do not auto-dispatch):**
- AI Recipe Generator (LLM "describe a scheme, get a recipe")
- Marketing landing page at `/` (currently redirects to /sign-in)
- Recipe public sharing URLs + OG images
- Recipe import from community sources

## Operating rules (read this carefully)

- **Working tree:** `D:\AI-Workstation\mini-manager`. Don't isolate worktrees; agents have lost their way doing that.
- **Two parallel agents max** on STRICTLY DISJOINT surfaces. Three+ caused stomping in Phase 14. When in doubt, serialize.
- **Local commit only.** Don't push from agents — Ross's instruction. Billy (you, the lead session) pushes after verifying.
- **Tests INTO feature commits.** No orphans.
- **Typecheck clean before each commit.** `npx tsc --noEmit` exits 0.
- **Solid-fill Button discipline.** Use the Button primitive with `variant` (success/warning/danger/special/secondary). `tone="outline"` only for low-emphasis.
- **No cyan on action buttons.** Phase 12 P12.23 + R7-6 + P13.1 + P13.10 all enforce this — don't regress.
- **No raw hex in components.** Use `@theme` tokens (`var(--color-X)`).
- **Push back when warranted.** Ross specifically wants this — co-creator relationship.
- **Lean optimistic.** Surface risks as items to monitor, never as reasons to expect failure. But tell the truth when the coin lands heads — Ross's competitive read on BrushForge ($15 lifetime, paint-focused only, no army planning) is correct; don't oversell or undersell.
- **Voice mode marker.** If Ross is on the TTS loop (`D:\AI-Workstation\claude-voice`) — usually says "speak" or has voice on — end every response with `🔊 <one concise sentence>`. The Stop hook speaks only that line aloud. Self-contained, natural to hear.
- **Default to single-select AskUserQuestion with 1-2 questions max.** Ross's UI mangles 4-up batches and multiSelect.

## Agents you'll use

- **`ui-builder`** — autonomous UI/UX backlog executor. Reads a markdown backlog (e.g., `docs/POST_LAUNCH_BACKLOG.md`), picks next unchecked, implements, tests, commits, ticks the backlog. Halts on architectural questions or tests it can't fix.
- **`milestone-builder`** — same loop but reads phase plans (`docs/PHASE15_PLAN.md` etc.) by milestone ID.
- **`ux-auditor`** — drives the live deploy in a real browser, writes `ux-audit/findings_vN.json` + `report_vN.md`. Returns a verdict 🟢 / 🟡 / 🔴. Mobile-only mode = focus the audit on 375/414/768 viewports.

When dispatching: explicit working directory, no isolation worktrees, clear list of files the agent owns vs concurrent agent's territory, "local commit only — do NOT push." Pass `run_in_background: true` for long runs.

## Recent corrections (don't repeat)

1. **Don't claim competitors are free without checking.** I claimed BrushForge was free; Ross corrected — it has a free Hobbyist tier + a paid Master tier ($15 lifetime). Always WebFetch competitor pricing pages before citing.
2. **Don't propose photo-to-palette as a new feature.** It's the eyedropper tool, already shipped.
3. **Activity heatmap (P14.6 — 90-day grid) is being replaced** with the heat-sink coverage grid per Ross's 2026-06-02 decision. The streak counter and activity stream stay; only the activity heatmap goes.
4. **FOCUS is feature-incomplete relative to the original brief.** Pinned recipe + notes + stopwatch + multi-recipe tabs shipped; current-slot indicator, step-done checkboxes, inline quick-actions, project-state pill, and recipe-completion % are still owed (P15.0).
5. **Two parallel milestone-builders on adjacent surfaces stomp on each other.** Cost ~20 min in Phase 14 mutual reverting. Use strict file disjointness or serialize.

## How Ross communicates

Belfast-born, lives in Los Angeles, US tax resident. ADHD + high IQ + dyslexic — typos are noise, ignore them. Direct co-creator relationship. He sets direction; you execute + push back where warranted. He'll often step away mid-build and trust the agents to keep shipping. When he says "lets keep going" interpret as "ship the next planned thing" — don't ask permission for every micro-decision, do ask for the architecturally-loaded ones.

Currently fatigued from a long session — that's why he's regrouping with a fresh chat. Don't burn his context on re-explaining things; he wants action.

## Suggested first move in the new chat

Confirm you have the handoff context loaded, then ask which to start with:
1. Write `docs/PHASE16_PLAN.md` for the heat-sink coverage grid (replacing activity heatmap)
2. Dispatch ui-builder for P15.0 FOCUS completion
3. Both in parallel (PHASE16_PLAN.md is a write task, P15.0 is build — no surface conflict)

Default to option 3 if he doesn't direct.

---

**Repo state at handoff time:**
- Last commit on origin/main: `8f7841d` "chore: Founder Edition price bump $19 → $26"
- 1587 unit tests passing
- One pending task: `#58 P10.4-P10.8 Stripe wire-up` (blocked on env vars)
- Working tree: clean (or near-clean — verify with `git status` in the new session)

Good luck, future Billy.
